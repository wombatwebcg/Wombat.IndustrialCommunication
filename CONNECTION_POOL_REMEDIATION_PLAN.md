# 连接池问题修复计划

## 目标

分三个阶段修复连接池中已确认的阻塞、并发和恢复问题。每个阶段必须独立编译、测试并可单独提交，避免一次性改动连接生命周期的多个维度。

## 阶段一：取消与异步关闭

### 范围

- 将 `CancellationToken` 从 `ResourcePool.AcquireAsync` 传递到条目获取、连接和重连路径。
- 为等待连接、重连和获取条目增加可取消行为。
- 增加异步租约释放和连接池释放入口，减少异步路径中的 `.GetAwaiter().GetResult()`。
- 保留现有同步 API 作为兼容包装，不改变公开调用方式。
- 明确关闭语义：取消只能终止等待，本地资源释放不能因取消而跳过。

### 验收标准

- 获取连接期间取消，可在合理时间内返回取消结果。
- 连接池释放不会无限等待正在连接或重连的条目。
- 原有同步 API 行为保持兼容。
- 增加获取取消、关闭取消和释放竞态测试。

## 阶段二：单连接并发调度

### 范围

- 将“并发已满立即失败”改为可取消的异步排队。
- 复用 `SemaphoreSlim` 控制每个连接条目的最大并发执行数。
- 强制关闭、连接池释放或调用方取消时，唤醒并终止等待者。
- 保持 `MaxConcurrentExecutionsPerEntry <= 0` 表示不限制的现有配置语义。
- 不新增全局任务队列或自定义调度框架。

### 验收标准

- 默认并发数为 1 时，同一连接上的并发请求按序执行，不再随机返回“执行并发已满”。
- 等待中的请求可以被调用方取消。
- 强制关闭不会留下永久等待的请求。
- 增加串行执行、并发上限、等待取消和强制关闭测试。

## 阶段三：故障恢复与租约收口

### 范围

- 用结构化故障信息替代英文错误消息匹配；优先使用异常类型和明确的错误分类。
- 保留消息匹配作为兼容兜底，并覆盖库内现有中文超时、断线消息。
- 让连接适配器返回结果时保留原始异常，供连接池判断是否可恢复。
- 评估并实现可自动归还的租约包装；内部业务调用继续优先使用 `ExecuteAsync` 的 `finally` 归还路径。
- 校正重试、恢复、租约过期和连接状态事件的一致性。

### 验收标准

- 超时、连接关闭、Socket/IO 异常和中文错误结果能够触发预期恢复。
- 参数、协议和业务错误不会被误判为可恢复故障。
- 租约重复释放安全，遗漏释放有明确的自动回收路径。
- 增加可恢复/不可恢复分类、重试次数、租约释放和事件顺序测试。

## 每阶段统一检查

1. `dotnet build Wombat.IndustrialCommunication.sln --no-restore`
2. 执行对应阶段的连接池单元测试，隔离依赖真实 PLC、串口或网络设备的集成测试。
3. 检查连接、关闭、维护任务和测试结束后无遗留后台任务。
4. 记录公开 API 兼容性变化；没有必要时不增加新抽象或依赖。

## 实施顺序

阶段一是阶段二可正确取消等待的前提；阶段二稳定业务执行边界后，再在阶段三调整失败恢复和租约生命周期。三个阶段不并行实施。
